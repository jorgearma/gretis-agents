{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "ClaudeReaderContext",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "improved_prompt",
    "selected_readers",
    "maps_used",
    "files_to_open",
    "files_to_review"
  ],
  "properties": {
    "improved_prompt": {
      "type": "string",
      "description": "Version mejorada y tecnicamente precisa del prompt original del operador."
    },
    "selected_readers": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": ["project-reader", "db-reader", "query-reader", "ui-reader",
                 "api-reader", "services-reader", "jobs-reader"]
      }
    },
    "maps_used": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "string",
        "enum": ["PROJECT_MAP.json", "DB_MAP.json", "QUERY_MAP.json", "UI_MAP.json",
                 "API_MAP.json", "SERVICES_MAP.json", "JOBS_MAP.json"]
      }
    },
    "files_to_open": {
      "type": "array",
      "description": "Archivos principales donde ocurre el cambio, con hints para el planner.",
      "items": { "$ref": "#/$defs/file_hint" }
    },
    "files_to_review": {
      "type": "array",
      "description": "Archivos de soporte, dependencias o referencias, con hints para el planner.",
      "items": { "$ref": "#/$defs/file_hint" }
    },
    "notes": {
      "type": "string",
      "description": "Observaciones relevantes para el planner: MAPs vacios, datos clave de modelos, contratos importantes."
    },
    "status": {
      "type": "string",
      "enum": ["ready", "blocked_no_maps", "blocked_pending_clarification"]
    },
    "dependency_graph": {
      "type": "object",
      "description": "Grafo forward de dependencias: clave es un archivo, valor es array de archivos que importa/usa. A -> [B,C] significa A depende de B y C.",
      "additionalProperties": { "type": "array", "items": { "type": "string" } }
    }
  },
  "$defs": {
    "file_hint": {
      "type": "object",
      "additionalProperties": false,
      "required": ["path"],
      "properties": {
        "path": {
          "type": "string",
          "description": "Ruta relativa al archivo."
        },
        "hint": {
          "type": "string",
          "description": "Descripcion breve del rol del archivo en el contexto de esta tarea."
        },
        "key_symbols": {
          "type": "array",
          "items": { "type": "string" },
          "description": "Funciones, clases o constantes que el planner debe buscar en este archivo. Obligatorio para files_to_open (excepto templates HTML)."
        },
        "test_file": {
          "type": ["string", "null"],
          "description": "Ruta del archivo de test asociado, extraida del MAP."
        }
      }
    }
  }
}
